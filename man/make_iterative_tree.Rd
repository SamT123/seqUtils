% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_iterative_tree.R
\name{make_iterative_tree}
\alias{make_iterative_tree}
\title{Build a phylogenetic tree iteratively with IQ-TREE bootstrap + CMAPLE growth}
\usage{
make_iterative_tree(
  alignment,
  tree_path,
  initial_iqtree_size,
  cascade_sizes,
  work_dir = NULL,
  unique_only = TRUE,
  seed = 100,
  num_threads = "AUTO",
  model = "GTR",
  return_tree = TRUE
)
}
\arguments{
\item{alignment}{A \code{data.frame}/tibble with columns
\code{Isolate_unique_identifier} (character), \code{dna_sequence} (character), and
\code{Collection_date} (Date or coercible). The cascade samples from this and
the final step uses it whole.}

\item{tree_path}{Character path for the final tree (Newick).}

\item{initial_iqtree_size}{Integer. Sequences per year for the IQ-TREE
bootstrap step.}

\item{cascade_sizes}{Integer vector of sequences-per-year for the CMAPLE
cascade steps. Must be strictly increasing.}

\item{work_dir}{Optional character. Directory for intermediate trees. If
\code{NULL} (default), uses \code{fs::path_dir(tree_path)}.}

\item{unique_only}{Logical. If \code{TRUE} (default), duplicate \code{dna_sequence}
entries are deduplicated before subsampling (forced-through tips
excepted).}

\item{seed}{Integer. RNG seed for subsampling and for IQ-TREE / CMAPLE.
Default 100.}

\item{num_threads}{Threads to pass to IQ-TREE and CMAPLE. Default
\code{"AUTO"}.}

\item{model}{Substitution model for both tools. Default \code{"GTR"}.}

\item{return_tree}{Logical. If \code{TRUE} (default), returns the final phylo;
else returns \code{tree_path}.}
}
\value{
A \code{phylo} object (default) or a character path.
}
\description{
Builds a high-quality phylogeny by bootstrapping a small backbone with
IQ-TREE, then growing it through a cascade of CMAPLE runs at increasing
per-year sample sizes, each freezing the previous tree. The final CMAPLE
run uses every row of \code{alignment}.

The cascade goes:
\enumerate{
\item IQ-TREE on \code{initial_iqtree_size} sequences/year.
\item For each \code{n} in \code{cascade_sizes}: CMAPLE on \code{n}/year, with
\code{starting_tree_path} = previous step's tree and
\code{freeze_starting_tree = TRUE}. Every tip from the previous tree is
forced into the new subsample.
\item A final CMAPLE on the entirety of \code{alignment}, again freezing the
previous tree.
}
Intermediate trees are written under \verb{<work_dir>/cascade/}.
}
\examples{
\dontrun{
aln <- readRDS("aln.RDS") # tibble with the required columns
tree <- make_iterative_tree(
  alignment           = aln,
  tree_path           = "tree.nwk",
  initial_iqtree_size = 10,
  cascade_sizes       = c(20, 40, 80, 160, 320, 640, 1280, 2560),
  work_dir            = "tree"
)
}
}
\seealso{
\code{\link[=make_iqtree_tree]{make_iqtree_tree()}}, \code{\link[=make_cmaple_tree]{make_cmaple_tree()}}
}
