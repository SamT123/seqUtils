% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_iterative_tree.R
\name{make_iterative_tree}
\alias{make_iterative_tree}
\title{Build a phylogenetic tree iteratively with IQ-TREE bootstrap + CMAPLE growth}
\usage{
make_iterative_tree(
  alignment,
  tree_path,
  initial_iqtree_size,
  cascade_sizes = NULL,
  starting_tree_path = NULL,
  starting_tips = NULL,
  work_dir = NULL,
  unique_only = TRUE,
  seed = 100,
  num_threads = "AUTO",
  model = "GTR",
  cmaple_path = NULL,
  iqtree_path = NULL,
  return_tree = TRUE
)
}
\arguments{
\item{alignment}{A \code{data.frame}/tibble with columns
\code{Isolate_unique_identifier} (character), \code{dna_sequence} (character), and
\code{Collection_date} (Date or coercible). The cascade samples from this and
the final step uses it whole.}

\item{tree_path}{Character path for the final tree (Newick).}

\item{initial_iqtree_size}{Integer. Sequences per year for the IQ-TREE
bootstrap step, and the seed for the default doubling cascade. When a
starting tree is supplied this is the per-year size that backbone
represents (no IQ-TREE is built).}

\item{cascade_sizes}{Integer vector of sequences-per-year for the CMAPLE
cascade steps; strictly increasing and all greater than
\code{initial_iqtree_size}. If \code{NULL} (default) it is generated by
\code{\link[=make_cascade_sizes]{make_cascade_sizes()}}; an empty schedule sends the backbone straight to
the full alignment.}

\item{starting_tree_path}{Optional character path to a pre-built backbone
tree (Newick). When supplied, the IQ-TREE step is skipped and this tree
seeds the cascade; \code{starting_tips} must accompany it.}

\item{starting_tips}{Character vector of the backbone tree's tip labels,
forced into every cascade subsample. Required when \code{starting_tree_path}
is supplied.}

\item{work_dir}{Optional character. Directory for intermediate trees. If
\code{NULL} (default), uses \code{fs::path_dir(tree_path)}.}

\item{unique_only}{Logical. If \code{TRUE} (default), duplicate \code{dna_sequence}
entries are deduplicated before subsampling (forced-through tips
excepted).}

\item{seed}{Integer. RNG seed for subsampling and for IQ-TREE / CMAPLE.
Default 100.}

\item{num_threads}{Threads to pass to IQ-TREE and CMAPLE. Default
\code{"AUTO"}.}

\item{model}{Substitution model for both tools. Default \code{"GTR"}.}

\item{cmaple_path}{Optional character. Directory containing the CMAPLE
executable, forwarded to \code{\link[=make_cmaple_tree]{make_cmaple_tree()}}. If \code{NULL} (default),
CMAPLE is taken from \code{PATH}.}

\item{iqtree_path}{Optional character. Directory containing the IQ-TREE
executable, forwarded to \code{\link[=make_iqtree_tree]{make_iqtree_tree()}}. If \code{NULL} (default),
IQ-TREE is taken from \code{PATH}. Unused when a starting tree is supplied.}

\item{return_tree}{Logical. If \code{TRUE} (default), returns the final phylo;
else returns \code{tree_path}.}
}
\value{
A \code{phylo} object (default) or a character path.
}
\description{
Builds a high-quality phylogeny by bootstrapping a small backbone with
IQ-TREE, then growing it through a cascade of CMAPLE runs at increasing
per-year sample sizes, each freezing the previous tree. The final CMAPLE
run uses every row of \code{alignment}.

A pre-built backbone can be supplied via \code{starting_tree_path} (with its tips
in \code{starting_tips}), in which case the IQ-TREE step is skipped and the
cascade grows that tree directly. \code{initial_iqtree_size} is then the per-year
size the backbone represents, used only to seed the doubling schedule.

The cascade goes:
\enumerate{
\item IQ-TREE on \code{initial_iqtree_size} sequences/year (skipped when
\code{starting_tree_path} is supplied).
\item For each \code{n} in \code{cascade_sizes}: CMAPLE on \code{n}/year, with
\code{starting_tree_path} = previous step's tree and
\code{freeze_starting_tree = TRUE}. Every tip from the previous tree is
forced into the new subsample.
\item A final CMAPLE on the entirety of \code{alignment}, again freezing the
previous tree.
}
Intermediate trees are written under \verb{<work_dir>/cascade/}.

When \code{cascade_sizes} is \code{NULL} (default) it is generated by
\code{\link[=make_cascade_sizes]{make_cascade_sizes()}} — doubling \code{initial_iqtree_size} until the densest
year is covered. That schedule may be empty (backbone straight to the full
alignment), in which case there are no intermediate cascade steps.
}
\examples{
\dontrun{
aln <- readRDS("aln.RDS") # tibble with the required columns
tree <- make_iterative_tree(
  alignment           = aln,
  tree_path           = "tree.nwk",
  initial_iqtree_size = 10,
  cascade_sizes       = c(20, 40, 80, 160, 320, 640, 1280, 2560),
  work_dir            = "tree"
)
}
}
\seealso{
\code{\link[=make_iqtree_tree]{make_iqtree_tree()}}, \code{\link[=make_cmaple_tree]{make_cmaple_tree()}}, \code{\link[=make_cascade_sizes]{make_cascade_sizes()}}
}
