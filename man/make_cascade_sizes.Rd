% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_iterative_tree.R
\name{make_cascade_sizes}
\alias{make_cascade_sizes}
\title{Doubling cascade sizes for iterative tree growth}
\usage{
make_cascade_sizes(alignment, start_size)
}
\arguments{
\item{alignment}{A \code{data.frame}/tibble with a \code{Collection_date} column
(Date or coercible).}

\item{start_size}{Integer. The per-year size the backbone already
represents (the doubling seed).}
}
\value{
A strictly-increasing integer vector, every element greater than
\code{start_size} and less than the largest per-year bin count; possibly empty.
}
\description{
Generates the per-year subsample sizes used by \code{\link[=make_iterative_tree]{make_iterative_tree()}} by
doubling \code{start_size} until the densest calendar year is covered:
\verb{2 * start_size, 4 * start_size, ...}, keeping only values strictly below
the largest per-year sequence count in \code{alignment}. Returns \code{integer(0)}
when \code{2 * start_size} already meets or exceeds that count, in which case the
caller grows the backbone straight to the full alignment.
}
\seealso{
\code{\link[=make_iterative_tree]{make_iterative_tree()}}
}
