% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date_outliers.R
\name{find_date_outliers}
\alias{find_date_outliers}
\title{Flag tips whose collection dates conflict with the phylogeny}
\usage{
find_date_outliers(tree, dates, iqd = 3, fit = NULL)
}
\arguments{
\item{tree}{A rooted \code{phylo} object.}

\item{dates}{A \code{Date} vector, one per tip, in \code{tree$tip.label} order.}

\item{iqd}{Interquartile ranges from the regression line beyond which a tip
is flagged.}

\item{fit}{Optional logical vector, one per tip: the tips that set the
line and IQR (e.g. an even subsample of an unevenly sampled tree). All
dated tips are still tested.}
}
\value{
A list with \code{outliers}, a tibble of \code{label}, \code{collection_date} and
\code{root_to_tip_residual_days} for the flagged tips, and \code{slope_per_day}, the
Theil-Sen slope in tree units per day.
}
\description{
Flags a tip whose root-to-tip divergence sits more than \code{iqd} interquartile
ranges from a Theil-Sen root-to-tip regression line. The slope is robust so
that the outliers being hunted do not distort the line they are tested
against, and is returned alongside the flags so a caller can reuse it as a
clock rate.
}
\details{
The test is invariant to the scale of the branch lengths, so the tree may be
in substitutions per site or in substitutions. Fewer than 20 dated tips
flags nothing, matching \code{chronumental}'s own guard.
}
