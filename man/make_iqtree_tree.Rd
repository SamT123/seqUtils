% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_iqtree_tree.R
\name{make_iqtree_tree}
\alias{make_iqtree_tree}
\title{Build a phylogenetic tree using IQ-TREE}
\usage{
make_iqtree_tree(
  sequences,
  tree_path,
  iqtree_path = NULL,
  num_threads = "AUTO",
  model = "GTR",
  seed = NULL,
  overwrite = FALSE,
  keep_files = c("nwk", "log"),
  return_tree = TRUE
)
}
\arguments{
\item{sequences}{Named character vector of DNA or RNA sequences. All
sequences must have unique names.}

\item{tree_path}{Character string specifying the file path where the tree
will be saved. A log file is also created at
\code{paste0(tree_path, ".log")}.}

\item{iqtree_path}{Optional character string specifying the directory
containing the IQ-TREE executable. If provided, this path is prepended to
\code{PATH}. If \code{NULL} (default), assumes \code{iqtree2} (or \code{iqtree}) is already
in \code{PATH}.}

\item{num_threads}{Character string or numeric specifying the number of
threads. Passed to IQ-TREE via \code{-T}. Default \code{"AUTO"}.}

\item{model}{Character string specifying the substitution model. Default
\code{"GTR"}.}

\item{seed}{Integer seed for the IQ-TREE RNG. \code{NULL} lets IQ-TREE choose.}

\item{overwrite}{Logical. If \code{TRUE}, passes \code{-redo} to IQ-TREE so it
overwrites existing output. Default \code{FALSE}.}

\item{keep_files}{Character vector specifying which files to keep after
IQ-TREE execution. Valid options are \code{"nwk"} (tree file), \code{"fasta"}
(alignment file), and \code{"log"} (log file). Default \code{c("nwk", "log")}.}

\item{return_tree}{Logical. If \code{TRUE} (default), returns the tree as a
\code{phylo}. If \code{FALSE}, returns the file path to the saved tree.}
}
\value{
A \code{phylo} object (default) or a character path.
}
\description{
Constructs a maximum likelihood phylogenetic tree from DNA/RNA sequences
using IQ-TREE 2. The tree is ladderized before being returned.
}
\examples{
\dontrun{
seqs <- c(seq1 = "ATCG", seq2 = "ATCC", seq3 = "TTCG")
tree <- make_iqtree_tree(seqs, tree_path = "iq_tree.nwk", seed = 1)
}
}
